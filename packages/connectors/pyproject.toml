[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nexus-connectors"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nexus-sdk>=0.1.0",
    "httpx>=0.28",
]
